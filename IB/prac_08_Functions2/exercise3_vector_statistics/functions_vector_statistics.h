#include <string>
#include <sstream>

void Introduction();
double ToCreateRandomNumbers(int& little_limit, int& big_limit, double& my_actual_random_number);
double StandardDeviation(double& actual_valor, double& average);
void FinalMessage1(int& first_number, int& second_number, int& third_number);
void FinalMessage2(double& average, double& standard_deviation, double& maximum, double& minimum);