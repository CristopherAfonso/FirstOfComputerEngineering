#include <iostream>
#include <string>

void Introduction() {
    std::cout << std::endl;
    std::cout << "Hola, soy un programa que viene de serie grabado con 10 palabras concretas, mi misión es mostrarte esas palabras y mostrarte por pantalla" << std::endl;
    std::cout << "cuatro características asociadas a cada una, y esas características son: Su longitud, Su primer caracter, su último caracter, y si la palabra contiene el texto 'abracadabra'" << std::endl << std::endl;
    std::cout << "Laas 10 palabras son: " << std::endl << std::endl;
}

void Introduction2() {
    std::cout << std::endl << std::endl;
    std::cout << "Ahora voy a mostrar por pantalla, las 4 características de cada palabra del vector" << std::endl << std::endl;
}

void Spaces() {
    std::cout << std::endl << std::endl << std::endl;
}